{% set AdminUI = Kernel.bundle('AdminUI') %}
{% extends AdminUI.getBasePageTemplate() %}
{% block page_content %}
    {% if tiles %}
        {% for tile in tiles %}
            {{ tile | raw }}
        {% endfor %}
    {% endif %}
{% endblock %}
